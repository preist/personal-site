# =================================
# Personal Website Configuration
# =================================

# Port Configuration
# ------------------
STRAPI_PORT=1337
SITE_PORT=3000

# Strapi Configuration
# --------------------
# IMPORTANT: Generate secure random keys for production!
# You can use: openssl rand -base64 32

# JWT Secret for authentication tokens
JWT_SECRET=DtvcWUE1A9Zt52qgtFvozQ==

# API Token Salt for securing API tokens
API_TOKEN_SALT=jGh0y+6yLOpJL5SQQ/VfwQ==

# Admin JWT Secret for admin panel authentication
ADMIN_JWT_SECRET=W/3ODj6YWiya/pUq1PuNFA==

# Transfer Token Salt for data transfer security
TRANSFER_TOKEN_SALT=t4mdt3YUOE7o5do9tfBlfw==

# App Keys for session management (comma-separated)
APP_KEYS=VbPxH9t3kiiBKeYFyU8mxg==,XjoQnbtGwlz2I+KF+EIkrw==,g/BiKCPL0ocR01tRlJGDKQ==,V7wSmNjsjL5ojVcxIP+vrA==

# Disable Enterprise Edition Promotion in Strapi
FLAG_PROMOTE_EE=false

# Next.js Configuration
# ---------------------
# URL where Strapi API is accessible from the frontend (client-side)
# Development: http://localhost:1337
# Production: https://api.yourdomain.com (or keep localhost for same-server setup)
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337

# Internal Docker network URL for server-side requests
# This is used when Next.js container needs to talk to Strapi container
STRAPI_INTERNAL_URL=http://strapi:1337

# =================================
# Production Security Notes
# =================================
#
# For production deployment:
# 1. Generate secure random values for all secrets
# 2. Use strong, unique values for each environment
# 3. Never commit actual secrets to version control
# 4. Consider using environment-specific .env files
#
# Generate secure keys with:
# openssl rand -base64 32
